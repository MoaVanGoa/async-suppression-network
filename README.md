# async-suppression-network
> Python-based asynchronous chip-level suppression network simulator. Event-driven nodes with thresholds, decay, and sparse random connectivity. Simulates asynchronous firing without a global clock. Optional visualization of node activity using matplotlib for analysis and demonstration.
